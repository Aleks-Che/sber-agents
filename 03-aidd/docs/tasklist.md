# План разработки: Кулинарный помощник в Telegram

**Дата создания:** 2025-12-06  
**Цель:** Создать работающего Telegram-бота с LLM-ассистентом для кулинарных вопросов.  
**Принципы:** KISS, итеративность, тестируемость каждого шага.

---

## 📊 Прогресс выполнения

| Итерация | Название                             | Статус       | Дата завершения | Примечания                                                |
| -------- | ------------------------------------ | ------------ | --------------- | --------------------------------------------------------- |
| 1        | Базовый бот с эхо-ответом            | ✅ Завершено | –               | Бот отвечает тем же сообщением                            |
| 2        | Интеграция с LLM (OpenRouter)        | ✅ Завершено | 2025-12-06      | Бот отвечает через LLM (OpenRouter)                       |
| 3        | Контекст диалога (история)           | ✅ Завершено | 2025-12-06      | Сохранение истории сообщений                              |
| 4        | Команды `/start`, `/help`, `/reset`  | ✅ Завершено | 2025-12-06      | Базовые команды управления                                |
| 5        | Специализированная команда `/recipe` | ✅ Завершено | 2025-12-06      | Реализована команда /recipe с специализированным промптом |
| 6        | Логирование и обработка ошибок       | ⏳ Ожидает   | –               | Улучшение отказоустойчивости                              |
| 7        | Деплой (опционально)                 | ⏳ Ожидает   | –               | Запуск на VPS/облаке                                      |

**Легенда статусов:**

- ✅ Завершено
- 🔄 В процессе
- ⏳ Ожидает
- 🚧 В разработке
- ❌ Отложено

---

## 📋 Итерационный план

Каждая итерация — это отдельный шаг, который можно протестировать и который добавляет новую функциональность. После завершения итерации обновляйте таблицу прогресса выше.

### Итерация 1: Базовый бот с эхо-ответом

**Цель:** Убедиться, что бот работает в Telegram и может принимать/отправлять сообщения.

**Задачи:**

- [x] Создать проект Python с uv и зависимостями (aiogram, python-dotenv)
- [x] Настроить токен бота через переменные окружения
- [x] Реализовать простейшего бота, который отвечает эхо-сообщением
- [x] Протестировать локально с помощью BotFather

**Тестирование:** Отправить боту любое сообщение и получить его же в ответ.

**Выходные артефакты:**

- Рабочий бот в Telegram
- Код в `src/cook_assistant/bot.py` (эхо-логика)
- Файл `.env` с токеном

---

### Итерация 2: Интеграция с LLM (OpenRouter)

**Цель:** Бот использует LLM для генерации осмысленных ответов на кулинарные вопросы.

**Задачи:**

- [x] Добавить клиент для OpenRouter API в `llm.py`
- [x] Реализовать системный промпт (кулинарный помощник)
- [x] Модифицировать бота: вместо эхо отправлять промпт в LLM и возвращать ответ
- [x] Настроить параметры LLM (модель, температура, макс. токены)
- [x] Обработать таймауты и сетевые ошибки (fallback-ответ)

**Тестирование:** Спросить бота «Как приготовить яичницу?» — получить связный рецепт от LLM.

**Выходные артефакты:**

- Модуль `llm.py` с классом `LLMClient`
- Бот, генерирующий ответы через LLM
- Конфигурация `OPENROUTER_API_KEY`

---

### Итерация 3: Контекст диалога (история)

**Цель:** Бот запоминает предыдущие сообщения в рамках одного чата для поддержания контекста.

**Задачи:**

- [x] Реализовать in‑memory хранилище истории (`storage.py`)
- [x] Ограничить длину истории (например, последние 10 сообщений)
- [x] Интегрировать историю в промпт (системный промпт + история + новый запрос)
- [x] Добавить очистку истории по команде `/reset` (позже)

**Тестирование:** Задать последовательные вопросы («Что можно приготовить из картофеля?» → «А с грибами?») — бот должен учитывать контекст.

**Выходные артефакты:**

- Модуль `storage.py` с классом `DialogStorage`
- Обновлённый `bot.py`, использующий историю
- Настройка `MAX_HISTORY_LENGTH`

---

### Итерация 4: Команды `/start`, `/help`, `/reset`

**Цель:** Добавить базовые команды для улучшения UX.

**Задачи:**

- [x] Реализовать команду `/start` — приветствие и краткое описание
- [x] Реализовать команду `/help` — список команд и примеры запросов
- [x] Реализовать команду `/reset` — очистка истории диалога для текущего чата
- [x] Обновить таблицу прогресс-команд в `README`

**Тестирование:** Вызвать каждую команду и убедиться в корректном ответе.

**Выходные артефакты:**

- Обработчики команд в `bot.py`
- Обновлённый `README` с инструкциями

---

### Итерация 5: Специализированная команда `/recipe`

**Цель:** Предоставить пользователю быстрый поиск рецепта по ключевым словам.

**Задачи:**

- [x] Добавить команду `/recipe <запрос>`
- [x] Сформировать специализированный промпт для рецептов (более детальный)
- [x] Вернуть ответ в формате: название, ингредиенты, шаги приготовления
- [x] При необходимости обрезать длинные ответы

**Тестирование:** Выполнить `/recipe паста карбонара` — получить структурированный рецепт.

**Выходные артефакты:**

- Обработчик команды `/recipe`
- Дополнительные промпт-шаблоны в `llm.py`

---

### Итерация 6: Логирование и обработка ошибок

**Цель:** Повысить надёжность и удобство отладки.

**Задачи:**

- [ ] Настроить логирование (модуль `logging`) с конфигурируемым уровнем
- [ ] Логировать входящие сообщения, запросы к LLM, ошибки
- [ ] Добавить глобальный обработчик исключений для бота
- [ ] Улучшить fallback-ответы при сбоях LLM

**Тестирование:** Сымитировать отключение сети — бот должен выдать понятное сообщение об ошибке и записать лог.

**Выходные артефакты:**

- Конфигурация логирования в `config.py` или `utils.py`
- Лог-файл `bot.log` (опционально)

---

### Итерация 7: Деплой (опционально)

**Цель:** Запустить бота на удалённом сервере для 24/7 работы.

**Задачи:**

- [ ] Написать `Dockerfile` и `docker-compose.yml`
- [ ] Настроить systemd/service для управления процессом
- [ ] Перенести хранилище контекста из памяти в Redis (для устойчивости)
- [ ] Настроить мониторинг (health checks, логи)

**Тестирование:** Бот должен работать после перезагрузки сервера и сохранять контекст.

**Выходные артефакты:**

- Docker-образ
- Конфигурация деплоя
- Скрипты запуска

---

## 🧪 Как тестировать каждую итерацию

1. **Локальный запуск:** `make run` (или `python -m src.cook_assistant.bot`)
2. **Открыть Telegram,** найти бота, отправить тестовое сообщение.
3. **Проверить логи** в консоли на наличие ошибок.
4. **Убедиться,** что функциональность соответствует описанию итерации.

После успешного теста отметить итерацию как завершённую в таблице прогресса.

---

## 📁 Структура проекта (напоминание)

```
cook-assistant/
├── .env
├── .env.example
├── Makefile
├── pyproject.toml
├── README.md
├── src/
│   └── cook_assistant/
│       ├── __init__.py
│       ├── bot.py
│       ├── llm.py
│       ├── config.py
│       ├── storage.py
│       └── utils.py
├── docs/
│   ├── idea.md
│   ├── vision.md
│   └── tasklist.md   (этот файл)
└── tests/            (будет добавлено позже)
```

---

## 🔄 Обновление прогресса

После завершения каждой итерации:

1. Обновите статус в таблице «Прогресс выполнения».
2. Поставьте галочки в задачах итерации.
3. Закоммитьте изменения с сообщением `feat: завершена итерация X`.

**Пример обновления таблицы:**

```markdown
| 2 | Интеграция с LLM (OpenRouter) | ✅ Завершено | 2025-12-07 | Бот отвечает через GPT-3.5 |
```

---

_Документ создан в рамках задачи «Создать пошаговый итерационный план разработки».  
Последнее обновление: 2025-12-06_
